import 'package:flutter/material.dart';

class faviorte extends StatelessWidget {
  const faviorte({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
      child: Text("this is faviorte page"),
    );
  }
}
